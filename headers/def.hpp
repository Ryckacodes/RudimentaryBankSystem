#pragma once
#include <string>

namespace global {
    std::ofstream acc;
    std::string line;
    std::string Username;
    std::string password;
    std::ofstream transfers;
    std::string options;

    std::ofstream balance;
}